# Ignore everything at the root
/*

# Allow directories so we can re-include files inside them
!*/

# Track C++ source files and markdown docs
!*.cpp
!*.md
!.gitignore

# Common editor and OS noise (kept for clarity; these are already ignored by the "ignore all" above)
.vscode/
build/
Debug/
Release/
*.o
*.out
*.exe
.DS_Store
